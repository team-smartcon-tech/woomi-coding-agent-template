---
type: log
updated: 2026-09-13
tags: [wiki/log]
---

# 기록

넣은 자료를 시간순으로 남깁니다. **덧붙이기만 하고 지우지 않습니다.**

나중에 "이건 왜 넣었더라"를 답하는 자리입니다.

---

## 2026-08-06 · 만들기 · 위키 볼트 생성

- 왜 만들었나: 바이브코딩 교육에서 받은 llm-wiki의 Obsidian 구조를 이 저장소에 맞게 적용하려고. `.agents/*` 규칙을 그래프로 탐색 가능하게 하고, 팀이 배운 것을 축적할 자리를 만들려고
- 구조: llm-wiki의 sources/wiki/log/index 구조를 계승하되, `wiki/wiki/` 중복을 피해 정리본 폴더를 `notes/`로 조정. 저장소 규칙 문서는 복사하지 않고 출처 기록이 실제 파일을 가리키게 함
- 명령: 루트 `.claude/commands/`에 `wiki-add-source`, `wiki-ask`, `wiki-log-today`, `wiki-check` 추가

## 2026-08-06 · 넣기 · AGENTS.md와 .agents/* 규칙 문서 19건

- 왜 모았나: 저장소 규칙을 위키 그래프로 연결해 온보딩과 "왜 이렇게 정했나" 검색에 쓰려고
- 원본: [색인](index.md)의 원본 목록 19건 (AGENTS.md, .agents/ 하위 규칙 문서)
- 새로 만든 페이지: [진입 규칙](rules/agents-entry.md), [바이브코딩 가이드](rules/vibe-coding-guide.md), [작업 흐름](rules/workflow.md), [절대 금지·필수 규칙](rules/non-negotiable-rules.md), [계층 아키텍처](rules/layered-architecture.md), [기술 스택](rules/stack.md), [프로젝트 구조](rules/project-structure.md), [API 계층 규칙](rules/api-layers.md), [코드 스타일](rules/code-style.md), [에러 처리](rules/error-handling.md), [테스트와 검증](rules/testing.md), [DB 스키마 가드레일](rules/db-schema-guardrails.md), [마이그레이션](rules/migration.md), [API 계약과 도메인 모델](rules/api-contract.md), [디자인 원칙](rules/design-principles.md), [UX 규칙](rules/ux-rules.md), [공통 컴포넌트](rules/components.md), [도구 사용 기준](rules/tooling.md), [배포](rules/deployment.md), [팀 위키](rules/team-wiki.md)

## 2026-08-13 · 정하기·배우기 · CLAUDE.md 경량화 + 위키 능동 사용

- 한 것: `CLAUDE.md`를 karpathy-skills 원칙으로 경량화(6장→4섹션, `AGENTS.md`와 겹치는 내용은 번호+제목 포인터로). `wiki/` 볼트가 있는 프로젝트에서 에이전트가 능동적으로 위키를 쓰도록 `AGENTS.md` §1에 지침을 넣음. PR #19 머지 → `v2.12-draft` 태그 자동 부착(2.11→2.12-draft).
- 정한 것과 이유:
  - 도구별 문서(`CLAUDE.md` 등)에는 `AGENTS.md`와 겹치는 규칙을 두지 않고 포인터만 둔다 — 규칙 1차 소스를 `AGENTS.md` 하나로 유지하려고(`AGENTS.md` §5의 적용).
  - 위키 사용 여부 게이트 = `wiki/` 폴더 존재. 별도 설정 토글을 만들지 않는다 — 안 쓰면 폴더를 지우면 되므로 machinery를 최소화하려고.
  - `CLAUDE.md`의 `AGENTS.md` 섹션 참조는 번호와 제목을 함께 적는다 — 섹션이 재번호돼도 제목으로 찾히게.
- 막혔다 푼 것:
  - 세션 초에 `wiki/`와 위키 명령이 안 보였다 → 로컬 HEAD가 위키 커밋 이전(`ad194b7`)이어서였고, 브랜치를 ff해 해결.
  - `gh pr merge`가 Claude Code auto-mode 분류기에 막혔다 → 사용자가 `!` 프리픽스로 직접 실행해 머지 완료.
- 갱신한 정리본: [팀 위키](rules/team-wiki.md), [진입 규칙](rules/agents-entry.md). 새 페이지 없음(갱신 위주).

## 2026-08-14 · 정하기·만들기 · 신규 사용자 온보딩(`/onboard`)과 git 승인 경계 재정의

- 한 것: 처음 온 비개발자를 첫 커밋까지 데려가는 `/onboard` 명령 추가. `AGENTS.md` §6의 git 승인 규칙을 작업 성격으로 나눔. `.claude/settings.json`에 로컬 git 작업 allowlist 신설. `QUICKSTART.md`의 `git init` 누락 수정. (2.12 → 2.13-draft)
- 정한 것과 이유:
  - **온보딩 종료 조건은 PR이 아니라 로컬 커밋 1개.** 공식 도입 경로가 ZIP 다운로드라 원격 저장소가 없다 — PR을 넣으면 GitHub 계정 만들기가 온보딩 안으로 들어와 온보딩이 GitHub 강좌가 된다. 단 브랜치 생성은 포함한다. 안 하면 `main`에서 커밋하게 되고 첫 푸시에서 반드시 차단당한다.
  - **`SessionStart` 훅을 만들지 않는다.** `CLAUDE.md`는 세션 시작 시 자동으로 읽히므로, 거기 한 줄 넣으면 훅 없이 같은 효과가 난다. 사용자가 "이거 뭐하는 폴더냐"고만 물어도 에이전트가 `/onboard`로 유도한다.
  - **git 승인 경계를 "승인 여부"가 아니라 "되돌릴 수 있느냐"로 나눈다.** 로컬 작업은 사전 승인, 저장소 밖으로 나가는 것만 한 줄 안내. 커밋을 매번 승인받게 하면 오히려 커밋을 미루게 되어 작업 유실 위험이 커진다.
  - **`scripts/agent-guard.cjs`와 `.githooks/`는 손대지 않는다.** 조사해 보니 하드 차단은 원래 되돌릴 수 없는 것(main 푸시, `--no-verify`, `reset --hard` 등)만 골라 막고 있었다. 평범한 `git commit`과 피처 브랜치 푸시는 처음부터 통과했다 — 마찰의 정체는 차단이 아니라 승인 규칙과 권한 프롬프트였다.
  - **allowlist는 `.claude/settings.local.json`이 아니라 추적되는 `.claude/settings.json`에 넣는다.** `CLAUDE.md`의 금지 규칙은 유지 — 금지 이유가 "리뷰에 안 보이는 곳에서 승인이 사라지는 것"이므로, PR에서 보이는 곳에 명시하면 그 이유에 저촉되지 않는다.
  - **실습 과제로 `dashboard.tsx`의 숫자를 쓰지 않는다.** 같은 값이 21~26행과 63~86행 두 곳에 있고 실제 렌더링은 아래쪽이다. 비개발자가 위를 고치면 화면이 안 바뀌어 첫날에 신뢰를 잃는다. 대신 `_app.tsx`의 사이드바 브랜드명을 쓴다 — 모든 화면에 보인다.
- 막혔다 푼 것:
  - "온보딩 문서가 없다"고 보고 새로 쓰려 했으나, `QUICKSTART.md`에 이미 AI 주도 프롬프트 A/B가 있었다 → 실제 결손은 문서가 아니라 **비개발자 경로의 단절 3개**(`git init` 누락 / GitHub 저장소 만드는 안내 0회 / 차단은 있는데 PR 만드는 법이 없음)였다. 조사 없이 만들었으면 있는 것을 또 만들 뻔했다.
  - `AGENTS.md` §6을 고치자 `.agents/WORKFLOW.md`와 위키 정리본 3개에 옛 규칙이 남아 모순이 생겼다 → 같은 작업에서 전부 동기화. 규칙을 고칠 때 어디가 그 규칙을 복제하고 있는지 먼저 grep해야 한다.
- 갱신한 정리본: [절대 금지·필수 규칙](rules/non-negotiable-rules.md), [작업 흐름](rules/workflow.md), [도구 사용 기준](rules/tooling.md). 새 페이지 없음.

## 2026-08-19 · 고치기 · `/wiki-check` 지적 3건 반영 (검증 명령 어긋남·필수 규칙 누락·출처 귀속)

- 왜 고쳤나: `/wiki-check` 점검에서 나온 9개 항목 중, 에이전트가 실제로 오작동할 수 있는 것부터 셋을 골라 고쳤다. 나머지(고아 페이지 3개, 원본 규칙 영역 누락 3건, `.agents/*` 4개 미수록)는 아직 남아 있다.
- 고친 것:
  - **검증 명령 어긋남** — [테스트와 검증](rules/testing.md)과 [배포](rules/deployment.md)가 `pnpm lint`·`deploy:dry`를 표준·필수 단계로 제시하고 있었다. 이 스캐폴드에는 둘 다 없다(루트 `package.json`은 `typecheck`·`test`·`build`만 제공, ESLint 설정 파일 없음). [기술 스택](rules/stack.md)은 정반대로 적고 있어 위키 안에서 모순이었다. 두 페이지에 실제 상태 단서를 넣고 세 페이지를 상호 링크로 묶었다.
  - **필수 규칙 1항목 복원** — [절대 금지·필수 규칙](rules/non-negotiable-rules.md)의 필수 목록이 `AGENTS.md` §6의 7개 중 6개만 담고 있었다. "반복되는 UX 패턴·공통 화면 정책은 `.agents/ui/UX_RULES.md`에 기록한다"가 빠져 있었다.
  - **출처 귀속 2건** — [팀 위키](rules/team-wiki.md)의 출처가 링크가 아닌 산문이었고(그래프에 선이 안 그려짐), [진입 규칙](rules/agents-entry.md)의 「이 프로젝트에서 정한 것」이 `AGENTS.md`에 없는 내용을 `AGENTS.md` 출처로 달고 있었다.
- 정한 것과 이유:
  - **`sources/`에 저장소 밖 자료 기록을 처음 만들었다** — [llm-wiki](sources/2026-08-19-llm-wiki.md). 본문을 받아 두지 않았으므로 "본문 미보관·대조 불가"를 기록 자체에 적었다. 출처를 지우거나 산문으로 남기는 대신 링크가 되게 한 이유는, 대조할 수 없다는 사실이 다음 점검에서 보여야 하기 때문이다. 본문을 확보하면 `/wiki-add-source`로 채운다.
  - **정리본이 원본을 바로잡는 쪽으로 썼다** — `lint` 어긋남의 1차 원인은 원본(`.agents/code/TESTING.md`, `.agents/DEPLOYMENT.md`)이 CTPA 레퍼런스 기준 4종을 적어 둔 것이다. 원본은 고치지 않고(`wiki/CLAUDE.md` 규칙), 정리본에 실제 상태를 덧붙였다. 근거는 `.agents/STACK.md` §7과 루트 `package.json`이라 위키 안에서 출처가 추적된다.
  - **`.agents/*`를 고치지 않았다** — 2.13-draft에서 추가된 `/onboard`가 `.agents/VIBE_CODING_GUIDE.md`에도 없다. 1차 결손이 원본 쪽이므로 위키를 먼저 고치면 규칙이 갈라진다. 원본을 고치는 작업은 `CHANGELOG.md`·표준 버전 갱신을 함께 요구하므로 따로 다룬다.
- 갱신한 정리본: [테스트와 검증](rules/testing.md), [배포](rules/deployment.md), [기술 스택](rules/stack.md), [절대 금지·필수 규칙](rules/non-negotiable-rules.md), [팀 위키](rules/team-wiki.md), [진입 규칙](rules/agents-entry.md). 새 원본 기록 1건([llm-wiki](sources/2026-08-19-llm-wiki.md)). 새 정리본 없음.

## 2026-08-19 · 넣기·고치기 · 위키 커버리지 완성과 남은 점검 지적 정리

- 왜 했나: 앞선 점검에서 "남은 것"으로 미뤄 둔 항목을 전부 닫으려고. 위키가 저장소 규칙 지도를 자칭하는데 `.agents/*` 4개가 아예 빠져 있어, 위키만 보면 지도가 완전한 것으로 읽히는 상태였다.
- 넣은 것: `.agents/*` 미수록 4건. 새 원본 기록 4개([NEST_GUIDE](sources/nest-guide.md), [NEST_CF_WORKER](sources/nest-cf-worker.md), [GOOD_EXAMPLES](sources/good-examples.md), [BAD_EXAMPLES](sources/bad-examples.md))와 새 정리본 2개([NestJS 프로젝트 규칙](rules/nestjs.md), [좋은 예시와 금지 예시](rules/examples.md)). **이제 `.agents/*` + `AGENTS.md` 23개가 23/23 수록됐다.**
- 고친 것:
  - **원본 규칙 영역 누락 3건** — [UX 규칙](rules/ux-rules.md)에 원본 16장 중 빠져 있던 7장(파일 업로드·이동과 현재 위치·키보드와 단축키·패널과 밀도·피드백·모바일·UX 문서화)을 넣고 선택·일괄 작업과 AI 체크리스트를 원본에 맞췄다. [공통 컴포넌트](rules/components.md)에 빠져 있던 컴포넌트 6개(Select·Checkbox·Field·PageHeader·StatCard·Placeholder)와 마크다운 렌더 규칙을 넣었다. [작업 흐름](rules/workflow.md)에 문서 업데이트 표와 최종 보고 형식을 넣었다.
  - **고아 페이지** — [README](README.md) 표의 `index.md`·`log.md`·`CLAUDE.md`가 백틱이라 그래프에 선이 안 그려졌다. 링크로 바꾸고 [색인](index.md)에서 README와 기록을 걸었다. **이제 혼자 떨어진 페이지 0개.**
  - **외부 자료 기록 형식** — [llm-wiki](sources/2026-08-19-llm-wiki.md)의 `원본:`이 산문이라 다른 23건과 형식이 갈렸다. `원본:`은 자료 이름, `본문: 미보관 — 대조 불가`를 별 필드로 분리했다.
  - **색인 문장의 순서 의존** — "마지막 1건만 저장소 밖 자료"가 목록을 재정렬하면 틀려진다. 목록에서 `(외부)`로 표시하는 방식으로 바꿨다.
- 정한 것과 이유:
  - **`sources/`의 `정리한 노트:` 줄은 갱신한다고 규칙에 명시했다** — [`wiki/CLAUDE.md`](CLAUDE.md). "읽기만 한다"(3단계)와 "양쪽 다 건다"(5단계)가 정면으로 충돌하던 자리다. "읽기만 한다"가 막으려는 건 자료 본문·원본 경로·목적을 고쳐 기록을 흐리는 것이고, 저 줄은 어느 정리본이 이 원본에서 나왔는지의 링크 색인이다. 갱신하지 않으면 한쪽만 걸린 링크가 구조적으로 남는다. 이 판정에 따라 [AGENTS.md 출처 기록](sources/agents-md.md)의 색인 줄에 팀 위키를 추가했다.
  - **볼트 밖 링크의 한계를 규칙에 적었다** — 볼트가 `wiki/`라서 `../../AGENTS.md` 류는 옵시디언에서 열리지 않는다. 고칠 수 있는 결함이 아니라 구조의 결과이므로, 정리본의 `## 출처`는 `sources/` 기록을 가리키는 것을 기본으로 하고 직접 링크는 예외로 둔다고 명시했다.
  - **NestJS 규칙을 한 정리본으로 묶었다** — 원본이 두 개(`NEST_GUIDE`, `NEST_CF_WORKER`)지만 적용 조건이 하나로 이어지고, 후자는 전자의 8장이 가리키는 add-on이다. [API 계약과 도메인 모델](rules/api-contract.md)이 원본 두 개를 묶은 것과 같은 방식.
  - **조건부 규칙임을 정리본 첫 줄에 박았다** — 이 스캐폴드는 React Router + Hono다. 조건 확인 없이 NestJS 규칙을 적용하면 [기술 스택](rules/stack.md)과 충돌한다. 위키에서 검색해 읽는 사람이 그 사실을 첫 줄에서 보게 했다.
- 갱신한 정리본: [UX 규칙](rules/ux-rules.md), [공통 컴포넌트](rules/components.md), [작업 흐름](rules/workflow.md), 그리고 새 정리본 2개와 짝을 맞추려고 `함께 보기`를 보강한 10개([진입 규칙](rules/agents-entry.md), [계층 아키텍처](rules/layered-architecture.md), [기술 스택](rules/stack.md), [API 계층 규칙](rules/api-layers.md), [프로젝트 구조](rules/project-structure.md), [코드 스타일](rules/code-style.md), [테스트와 검증](rules/testing.md), [마이그레이션](rules/migration.md), [팀 위키](rules/team-wiki.md), [DB 스키마 가드레일](rules/db-schema-guardrails.md), [API 계약과 도메인 모델](rules/api-contract.md)).

## 2026-08-19 · 고치기 · 위키가 찾아낸 원본 결손 2건 (2.13 → 2.14-draft)

- 왜 했나: 앞선 점검에서 "1차 결손이 원본 쪽이라 위키를 먼저 고치면 규칙이 갈라진다"고 미뤄 둔 것을 닫으려고. 위키가 `.agents/*`의 사본이 아니라 **대조 장치**로 작동한 사례다 — 정리본을 원본과 맞추려다 원본의 구멍을 찾았다.
- 고친 원본:
  - **`.agents/VIBE_CODING_GUIDE.md`에 `/onboard`가 없었다.** 2.13-draft에서 명령을 만들었는데, 처음 오는 사람이 실제로 읽는 2.3절·4.3절에는 여전히 `QUICKSTART.md` 프롬프트와 `/new-feature` 계열만 있었다. `/onboard`를 알 방법이 에이전트용 `CLAUDE.md`뿐이었다. 4.3절에 위키 명령 4종도 함께 등재했다.
  - **`/wiki-add-source`가 스스로 모순이었다.** 2단계 "sources는 읽기만 한다"와 5단계 "양쪽 다 건다"가 `정리한 노트:` 줄에서 충돌한다. 5단계를 따르면 2단계를 어기고, 2단계를 지키면 한쪽만 걸린 링크가 구조적으로 남는다. 그 줄은 색인이므로 갱신한다고 명령과 [`wiki/CLAUDE.md`](CLAUDE.md) 양쪽에 적었다. 자료 본문 미보관 표기도 규약으로 넣었다.
- 정한 것과 이유:
  - **원본을 고칠 때는 `CHANGELOG.md`와 표준 버전을 함께 올린다** — `AGENTS.md` §10. 위키만 고치던 앞선 두 작업과 갈리는 지점이다(위키 변경은 표준 버전을 올리지 않는다). 2.13 → **2.14-draft**, 최종 수정일 2026-08-19. 태그는 머지 시 워크플로우가 붙이므로 직접 만들지 않는다.
  - **명령 문서와 위키 규칙 양쪽에 같은 판정을 적었다** — 한쪽만 고치면 다음 사람이 다른 쪽을 근거로 반대로 판단한다. 규칙을 고칠 때 어디가 그 규칙을 복제하고 있는지 먼저 찾는다는 것은 2026-08-14 항목에서 이미 배운 것이다.
- 갱신한 정리본: [바이브코딩 가이드](rules/vibe-coding-guide.md) — 원본에 `/onboard`가 들어갔으므로 같은 작업에서 동기화.

## 2026-08-30 · 정하기 · 지식 종류별로 폴더를 갈랐다 (파생 프로젝트에서 역수입)

- 계기: 이 템플릿에서 분기한 프로젝트(Smart Planner)가 위키를 전수 점검했더니, 구조는 성한데(끊긴 링크 0·고아 0·색인 일치) **낡은 정리본이 전부 규칙 페이지**였고 한쪽만 걸린 링크도 **전부 도메인→규칙 방향**이었다. 규칙 정리본과 화면 지식이 `notes/` 한 폴더에 섞여, 새 화면 문서는 갱신되고 규칙 정리본만 방치되는 구조였다
- 정한 것: `notes/` → **`rules/`** 로 이름을 바꾸고, **`systems/`**(화면·경로)와 **`patterns/`**(반복 실패)를 규약으로 추가한다. **다만 이 저장소에는 빈 폴더로 만들지 않는다** — 스캐폴드에는 아직 정리할 화면도 두 번 이상 반복된 실패도 없다. 분류만 미리 만들면 채우려는 압력이 생기고, 사례 1건짜리 패턴 페이지가 늘어난다
- **패턴은 사례 2건 이상일 때만** 만든다는 기준을 규칙으로 박았다. 파생 프로젝트에서 실제로 4건이 나왔는데(조용한 성공·고쳤는데 그대로다·문서가 실동작과 어긋난다·코드와 DB 가 따로 나간다) 전부 **새로 조사한 게 아니라 이미 흩어져 있던 사례를 접은 것**이었다. 접을 것이 없으면 페이지도 없다
- frontmatter(`type`/`updated`/`tags`) 도입. `updated` 는 **내용이 바뀐 날**로 정의한다 — 이번 이동·형식 정리에는 갱신하지 않았고, 그래서 각 정리본이 언제 멈췄는지가 날짜만으로 드러난다. 원본(`.agents/*`) 최종 수정일과 대조하는 검사를 `/wiki-check` 10번 항목에 넣었다
- `commit-wiki` 훅 도입(파생 프로젝트에서 검증됨) — 커밋 직후 방금 커밋에 코드·규칙 파일은 있는데 위키 파일이 없으면 기록을 요구한다. 실작업 커밋 26건이 위키 갱신 0건으로 지나간 사례가 계기였다. **단 이 훅은 "위키를 남겼나"만 보고 "낡았나"는 못 본다** — 그건 아직 `/wiki-check` 를 주기적으로 도는 수밖에 없다
- **`/wiki-check` 4번에 "`sources/` 도 반드시 스캔"을 박았다.** 파생 프로젝트가 `notes/`→`rules/` 이름을 바꾸면서 출처 노트의 `정리한 노트:` 줄 21개를 놓쳤는데, 검사 스크립트가 `sources/` 를 스캔에서 빼놔 "끊긴 링크 0건"으로 통과했다. **검사에서 뺀 폴더는 검사한 것이 아니다**
- `AGENTS.md` 10장에 **분기 프로젝트의 제품 버전 분리 지침**을 넣었다. 이건 실제로 사고가 났다 — 파생 프로젝트가 템플릿 번호 라인을 그대로 이어 써서 `2.14-draft` 가 두 저장소에서 서로 다른 내용을 가리키게 됐고, 양쪽에 태그까지 붙었다. 분기하면 제품 버전(`MAJOR.MINOR.PATCH`, `1.0.0` 은 운영 전환)과 기반 템플릿 표준을 **두 축으로 나눠 적는다**
- 가져오지 않은 것: 파생 프로젝트의 `patterns/`·`systems/` **내용**(그쪽 커밋·사고 기록이라 템플릿에 맞지 않는다)과 `AGENTS.md` 7장의 `deploy:dry` 정정(**이 저장소 루트 `package.json` 에는 실제로 없다** — 파생 쪽이 Cloudflare 배포를 하면서 추가한 스크립트다. 같은 문장이라도 저장소마다 참·거짓이 갈린다)

## 2026-09-13 · 고치기 · 위키 명령을 Codex·Copilot 면으로 이식 (2.16 → 2.17-draft)

- 계기: 하네스 구조 점검. 실행 검사(`agent-guard --selftest`·`pnpm typecheck`·`pnpm test`)는 전부 통과했고, 드리프트는 **파일 목록을 세어 보고서야** 드러났다 — 명령이 `.claude/commands/` 9개인데 `.codex/prompts/`·`.github/prompts/`는 4개였다.
- 고친 것:
  - `wiki-add-source`·`wiki-ask`·`wiki-check`·`wiki-log-today` 를 Codex·Copilot 면에 이식(8파일). 기존 규약 그대로 — Codex 판은 바이트 동일, Copilot 판은 frontmatter 에 `mode: agent` 한 줄만 추가. `diff` 로 나머지 차이 0 확인.
  - `README.md` — "Codex·Copilot 에 같은 워크플로우가 있습니다"가 **거짓이었다**(실제 4/9). 이식으로 참이 되었고 유일한 예외 `/onboard` 를 문장에 박았다.
  - `AGENTS.md` §1 — "Claude Code는 …, 명령이 없으면 직접 편집" 단서를 "세 도구 모두에 있다"로.
  - `.gitignore` 에 `/.obsidian/` — 저장소 루트를 볼트로 연 흔적이 늘 untracked 로 남아 있었다. 위키 볼트는 `wiki/` 이고 `wiki/.obsidian/graph.json` 은 추적 유지라 앵커를 `/` 로 시작해 루트만 막았다.
- 정한 것과 이유:
  - **요구와 수단은 같은 면에 있어야 한다.** `commit-wiki` 훅은 `.codex/hooks.json` 에도 등록되어 Codex 에서 똑같이 위키 기록을 요구하는데, 그걸 수행할 명령은 Claude 면에만 있었다. 훅을 세 면에 걸면서 명령은 한 면에만 두는 것이 이 어긋남의 모양이다.
  - **드리프트 시점을 추정하지 않고 실측했다.** `git log --diff-filter=A` 로 `70e75a8`(2.11-draft, 2026-08-06)에서 네 파일이 한 번에 `.claude/commands/` 에만 들어왔음을 확인했다. 6주간 아무도 몰랐다. 처음 CHANGELOG 초안에는 "2.13·2.15 커밋에서" 라고 적었다가 실측하고 고쳤다 — **커밋을 짚기 전에 로그를 본다.**
  - **`wiki/CLAUDE.md` 경로는 그대로 뒀다.** 파일명이 Claude 를 가리키지만 실제로는 위키 폴더 규칙 파일이고, 이식된 명령들은 필요한 지점에서 이 경로를 인라인으로 가리킨다. 이름을 바꾸면 `commit-wiki` 훅 메시지·`wiki/README.md`·정리본 출처 링크까지 따라가야 한다 — 이번 작업의 값어치를 넘는다.
- 남은 것: **세 면의 패리티를 검사하는 장치가 없다.** 이번에도 사람이 세어서 찾았다. `scripts/agent-guard.cjs` 에 패리티 판정을 더해 `--selftest` 에 넣는 것이 다음 후보다.
- 갱신한 정리본: [팀 위키](rules/team-wiki.md) — 「네 동작」에 세 면 이식 사실과 그 어긋남의 모양을 적었다.

## 2026-09-13 · 정하기 · Prettier·ESLint 를 표준으로 넣었다 (2.17 → 2.18-draft)

- 계기: "스마트플래너의 `eslint-config-prettier` 규정을 여기 적용할지" 를 검토하다가, 그 패키지는 ESLint 없이는 no-op 이라 **"린터를 넣을 것인가"** 가 진짜 질문임이 드러났다. 이 저장소는 `.agents/STACK.md` 에 "ESLint 는 설치하지 않는다" 를 명시적으로 적어 둔 상태였다.
- **처음 권고가 틀렸고, 사용자가 바로잡았다.** 실측해 보니 ESLint 최소 구성 지적이 **0건**이라 "미룬다" 고 권고했는데, 사용자가 "코딩 표준 템플릿이니 앞으로 할 작업에 필요하면 미리 반영하는 게 낫지 않나" 고 되물었다. 맞다 — **0건은 지금 쌓인 빚을 잰 값이고, 린터의 값은 앞으로 생길 빚을 막는 것**이다. 이 저장소는 파생 프로젝트의 출발점이라 정확히 그 값이 본체다. 증거가 스마트플래너 자신이다: 도입 시점에 `rules-of-hooks`·`exhaustive-deps` 가 0건이었다 — 처음부터 켜져 있었다면 4.7만 줄에서 352건을 재고 오탐을 걸러내는 작업이 아예 없었다.
- 정한 것과 이유:
  - **스캐폴드의 버그는 파생 프로젝트 수만큼 복제된다.** `login.tsx` 의 `String(form.get("email") ?? "")` 가 `[object File]` 을 흘리는데 **타입 검사가 못 잡는다**(`String()` 은 어떤 값이든 받는다). 스마트플래너에서 같은 패턴이 43곳까지 번졌고 그 출발점이 이 파일이다. `formString()` 헬퍼 + 테스트 4건으로 고쳤다 — 린터를 넣는 것보다 이게 먼저였다.
  - **값이 아니라 방법을 물려준다.** `printWidth 100`·"어떤 규칙을 켜는가" 는 이 코드베이스 실측값이고, 복사해 간 프로젝트는 다른 코드베이스가 된다. `.agents/STACK.md` 에 값과 **그 값을 어떻게 정했는지**를 함께 적고, "recommended 세트를 통째로 펼치지 말고 지적 건수를 먼저 재라" 를 규칙으로 박았다.
  - **`endOfLine: "auto"` 는 그대로 복사해도 됐다 — 추정이 아니라 실측했다.** `.gitattributes` 가 `*.md`·`*.json`·`*.yml` 만 `eol=lf` 로 잡고 `.ts`/`.tsx` 는 빠져 있어, `core.autocrlf=true` 환경에서 작업 트리가 CRLF 다. 양쪽 저장소가 같은 조건이라 값이 같았다. **같은 문장이라도 저장소마다 참·거짓이 갈리므로 복사 전에 잰다.**
  - **설정만 넣고 게이트를 안 만들면 규칙이 아니라 권고다.** 이 저장소의 PR 필수 검사는 0개였다(`tag-version.yml` 은 태그만 붙인다). `pr-checks.yml` 을 함께 넣었고, 로컬 훅이라 CI 가 유일한 검사인 `agent-guard --selftest` 도 스텝으로 넣었다.
  - **`.prettierignore` 에서 마크다운을 뺀 건 이 저장소에서 특히 중요하다** — 문서가 본체다(`.agents/` 22종 + 위키 46면 + 루트 5종). 한글 표를 prettier 가 글자 수로 정렬하면 오히려 어긋나고, 한 줄만 고쳐도 표 전체가 재정렬된다.
- 실측: 47파일 2,702줄 · 재포맷 21파일 +1,669/−482 · ESLint 0건 · 세미콜론 163/2,702 · 큰따옴표 import 163 vs 2. 스마트플래너 비교값은 268파일 47,091줄 · 재포맷 163파일 · ESLint 약 57건.
- 검증: `format:check` · `typecheck` · `lint` 0건 · `test` 13건 · `build` · `agent-guard --selftest` 전부 통과. **재포맷 뒤에 `agent-guard.cjs` 자체 점검을 다시 돌린 게 중요했다** — 그 파일도 재포맷 대상이라 정규식 리터럴의 이스케이프가 깨졌을 수 있었다.
- **검증하지 못한 것**: `pr-checks.yml` 은 아직 GitHub 에서 돌아 본 적이 없다. PR 을 올려야 확인된다.
- 남은 것: 재포맷 커밋을 설정 커밋과 분리해 만들고, 그 SHA 를 `.git-blame-ignore-revs` 에 적어야 한다(파일에 TODO 로 남겨 뒀다).
- 갱신한 정리본: [포맷과 린트 기준](rules/formatting-and-linting.md) **신규**, [기술 스택](rules/stack.md)·[코드 스타일](rules/code-style.md)(원본이 바뀌어 동기화), [테스트와 검증](rules/testing.md)(검증 명령).

## 2026-09-13 — 점검 (`/wiki-check`)

- 14개 항목 중 **실제 결함 5종 29건**. 나머지 9항목 0건. **대부분이 같은 날 내가 만든 것이거나 내가 3배로 복제한 것이었다.**
- 고친 것:
  - **frontmatter 규약 모순 (24건의 뿌리).** [`README.md`](README.md)·[`CLAUDE.md`](CLAUDE.md) 는 "모든 페이지에 `type`/`updated`/`tags`" 라고 했는데 `/wiki-add-source` 는 원본에 `원본`/`넣은날`/`목적` 만 적으라고 지시하고 있었다. `sources/` 24개 전부가 명령을 따르고 README 를 어긴 것처럼 보였다 — **명령이 맞고 README 가 틀렸다.** 원본은 넣은 뒤 고치지 않으므로 `updated` 가 의미를 갖지 않는다. README 에 "`sources/` 는 형식이 다릅니다" 절을 만들고, CLAUDE.md 에 한 줄, 명령 3면(`.claude`·`.codex`·`.github`)에 `type: source` 를 명시했다. **방증이 있었다 — `_templates/` 에 `source` 템플릿만 없다.**
  - **틀린 수치 2건.** [`index.md`](index.md) "규칙 정리본 22개" → 23, [포맷과 린트 기준](rules/formatting-and-linting.md) "`wiki/` 46면" → 47. 둘 다 같은 날 내가 페이지를 하나 더하면서 목록만 고치고 개수를 안 고친 것이다. **같은 턴에 임원 덱의 46 은 47 로 고쳤으면서 내 새 페이지 안의 같은 수치는 놓쳤다.**
  - **`updated` 미갱신 4건** — `testing.md`·`workflow.md`·`index.md`·`log.md`. 내용을 고쳐 놓고 안 올렸다.
  - **한쪽만 걸린 링크 1건** — `formatting-and-linting.md` → `workflow.md` 역방향 없음. 전날 세 곳만 양방향으로 걸고 하나를 빠뜨렸다.
- 정한 것과 이유:
  - **규약이 두 문서에서 갈리면 24개 파일이 아니라 문서 한 줄을 의심한다.** 기계 검사는 "24건 위반" 으로 보고하지만, 24개가 일관되게 같은 형식이면 그건 위반이 아니라 **다른 규약을 따른 것**이다. 문서 쪽을 먼저 읽어 어느 쪽이 맞는지 정한 다음에 고친다.
  - **명령을 고칠 때는 3면을 같이 고친다.** 2.17-draft 에서 위키 명령을 Codex·Copilot 면에 이식했으므로 이제 규약 변경도 3배다. `diff` 로 패리티를 다시 확인했다.
- 확인했으나 결함이 아니었던 것:
  - **낡음 후보 2건 해제.** `agents-entry.md`·`non-negotiable-rules.md`(둘 다 `updated` 08-19) 가 `AGENTS.md`(커밋 08-31)보다 오래돼 ⚠ 로 떴지만, `git log -1 -p -- AGENTS.md` 로 보니 그 커밋(`bb14be3`)은 **§4 디렉터리 트리의 `docs/` 한 줄만** 바꿨다. 두 페이지가 정리하는 §1·§2·§5·§6 은 무관하다. **날짜 비교는 후보를 뽑는 도구지 판정이 아니다.**
  - **고쳐진 원본 0건.** `sources/` 24개 전부 `git log --follow` 로 봤다. 커밋이 2개 이상이지만 내용은 `type: source` 추가(`86e7ecb`), 외부 자료 1건의 `원본:` 표기 규약(`aeba8bd`·`6c74773`), `notes/`→`rules/` 경로 치환뿐이다. 마지막 것은 `정리한 노트:` 줄이라 [`README.md`](README.md) 표가 **명시한 예외**다.
- **확인하지 못한 것**: 정리본 23개를 원본 `.agents/*` 와 전수 대조하지 않았다(표본 2건만). "목록을 복사했나" 는 grep 으로 0건이지만 "뉘앙스를 왜곡했나" 는 못 봤다.

## 2026-09-13 · 정리하기 · `.userdocs/` 를 없애고 `docs/` 로 합쳤다 (2.18 → 2.19-draft)

- 계기: "`docs/` 가 있는데 `.userdocs/` 는 왜 있나" 라는 질문. 답을 찾으려 이력을 보니 **순서가 반대였다** — `.userdocs/` 가 `41f8f85`(하네스 템플릿 도입) 때 먼저 있었고 `docs/` 는 2주 전 `2.16-draft` 에 처음 내용이 들어갔다. `AGENTS.md` 4장이 최근까지 "`docs/` — 필요할 때 생성, 아직 없음" 이라고 적고 있었다. **둘의 관계를 정리한 적이 없었던 것이지, 나눠 둘 이유가 있었던 게 아니다.**
- 정한 것과 이유:
  - **합친다.** 남은 구분("만들어 내는 산출물" 대 "설계에 들어간 기록")은 약했고, 점 디렉터리인 것이 특히 맞지 않았다 — 이 저장소의 다른 점 디렉터리는 전부 도구 설정인데 `.userdocs/` 만 사람이 읽는 문서라 `ls`·탐색기·검색에서 기본으로 숨었다. 남은 파일이 **실질 1개**라 지금이 가장 쌌다.
  - **`docs/README.md` 를 새로 만들지 않았다.** `docs/` 의 내용은 `README.md` 문서 지도와 `AGENTS.md` 4장이 이미 가리킨다. 색인을 하나 더 만들면 **낡을 것을 하나 더 만드는 것**이다 — 이번에 지운 `.userdocs/README.md` 가 정확히 그렇게 됐다.
  - **과거 CHANGELOG 의 경로 인용은 고치지 않았다.** 그때의 사실이고 태그가 붙었다. 사람이 따라갈 만한 한 곳(`2.8-draft`)에만 새 위치를 괄호로 덧붙였다.
- **방치의 증거가 결정적이었다.** `.userdocs/README.md` 가 나열한 4개가 **하나도 없었다**(둘은 `32f6176` v2.7-draft 에서 삭제됐는데 README 를 안 고쳤고, 실제로 있던 평가 보고서는 표에 없었다). 그리고 `AGENTS.md` 4장에는 `.userdocs/` 가 아예 없는데 `README.md` 트리에는 있었다 — **두 문서가 다른 구조를 그리고 있었다.** 폴더의 존재 이유를 묻기 전에 이미 아무도 안 보고 있었다는 뜻이다.
- **디렉터리를 없앨 때는 참조부터 센다.** `grep -rn userdocs` 로 8곳이 나왔다 — `.prettierignore`, `agent-guard.cjs` selftest 케이스, `AGENTS.md` 두 곳, `README.md` 두 곳, `QUICKSTART.md`, `.agents/STACK.md`, 정리본. 훅 동작은 안 바뀐다(`CHANGELOG_SCOPE` 정규식에 `.userdocs` 가 애초에 없었다) — 바뀐 건 selftest 케이스 한 줄이다.
- 지운 것: 사용자 요청으로 `.userdocs/외관디자인기준_앱_재구축계획.{md,docx}` 2개를 삭제했다. **git 이력 0건이라 복구 불가**임을 확인하고 지웠다.
- 갱신한 정리본: [포맷과 린트 기준](rules/formatting-and-linting.md) — 포맷 제외 목록에서 `.userdocs/` 제거.

## 2026-09-13 · 정하기 · 브라우저 검증을 표준 절차로 넣었다 (2.19 → 2.20-draft)

- 계기: `docs/` 에 미추적 파일 하나가 있었다 — `2026-09-06-브라우저-검증-절차.md`. 스마트플래너 원본과 **바이트 동일**했고 git 이력 0건이었다. 언젠가 복사만 해 두고 편입하지 않은 것이다. 사용자가 "표준 템플릿에 추가해봐" 라고 해서 각색해 정식으로 들였다.
- 정한 것과 이유:
  - **자동 검사 다섯은 "눌러 보면 되는가"를 증명하지 않는다.** 근거는 파생 프로젝트의 실제 사례다 — **테스트 490건이 전부 통과하는 상태로** 마일스톤 바 드래그가 잘못 동작했다(바 폭 10px 중 크기조절 핸들이 6px 이라 이동 영역이 4px 뿐이었다). `2.18-draft` 에서 CI 를 넣었지만 **검사만 하고 배포하지 않으므로** PR 브랜치를 띄워 볼 자리가 없다. 로컬 브라우저 검증이 유일한 경로다.
  - **문서만 두지 않고 규칙에 걸었다.** `AGENTS.md` 7장 Quality Gates 와 `.agents/code/TESTING.md` 양쪽에 넣었다. `docs/` 에만 있으면 아무도 안 읽는다 — 이번에 그 파일이 일주일간 미추적으로 방치돼 있던 게 그 증거다.
  - **그대로 복사하지 않았다.** 파생 프로젝트 절차의 두 곳이 이 스캐폴드와 맞지 않았다. ① 로그인 — 파생은 세션 쿠키를 직접 만들었는데 이 스캐폴드는 **데모 계정 3개**가 있어 폼 입력이면 끝이다(쿠키 위조는 "실제 인증을 붙인 뒤" 용으로 접은 절에 옮겼다). ② 저장 확인 — 파생은 Supabase 조회였는데 이 스캐폴드는 **인메모리 목업**이라 "새로고침 후에도 남아 있는가" 까지가 범위다. **같은 문장이라도 저장소마다 참·거짓이 갈린다.**
  - **파일명에서 날짜를 뺐다.** 특정 시점의 기록이 아니라 계속 쓰는 기준이다.
- 설치 안내에서 정한 것:
  - **"DevTools 설치"는 오해다** — 크롬·엣지에 내장되어 있다(`F12`). 이 절차가 쓰는 건 DevTools **프로토콜**이다. 이 구분을 문서 첫 줄에 박았다.
  - **엣지를 먼저 확인한다.** 같은 CDP 로 그대로 동작하므로 "크롬이 없다"는 이유로 새로 설치할 일이 아니다. 함정 표에도 넣었다.
  - **에이전트가 브라우저·확장을 임의로 설치하지 않는다.** 왜 필요한지 사용자 언어로 설명하고 승인을 받는다. 확장은 특히 브라우저 권한을 주는 일이다.
  - **Claude for Chrome 은 편의 수단이지 요구사항이 아니다.** 제공 범위가 요금제·지역에 따라 제한될 수 있어 단정하지 않고 공식 안내를 가리켰다. **없어도 절차가 그대로 돈다**는 것을 명시했다 — 안 그러면 확장이 없는 사람이 검증을 건너뛴다.
- **확인하지 못한 것**: 이 절차를 이 스캐폴드에서 처음부터 끝까지 실행해 보지 않았다. 데모 계정·인메모리 목업 부분은 코드를 읽고 각색한 것이고, CDP 단계는 파생 프로젝트에서 검증된 내용 그대로다.

## 2026-09-14 · 정하기 · 패리티 검사 장치는 만들지 않기로 했다

- 전날 "남은 것" 으로 적어 둔 **세 면 명령 패리티 검사**를 만들지 묻자, 필요한지부터 되물어 왔다. 실측하니 **내 제안이 성급했다.**
- 근거: 저장소 5개월(커밋 82) 동안 **명령 파일이 추가·삭제된 커밋은 4건**이다 — `b5c644d`(초기, 세 면 다) · `70e75a8`(위키 명령, Claude 만 ← 유일한 드리프트) · `e9b2f41`(`/onboard`, **의도된** Claude 전용) · `5e63628`(이번 이식). **드리프트는 1건이고 이미 고쳤다.**
- 정한 것과 이유:
  - **n=1 에는 만들지 않는다.** 이 저장소가 스스로 정한 기준이다 — [`wiki/README.md`](README.md) "`patterns/` 는 사례가 2건 이상 모였을 때만 만듭니다". 2.15-draft 가 그 기준을 박은 이유(*"분류만 미리 만들면 채우려는 압력이 생긴다"*)는 검사 장치에도 그대로 적용된다.
  - **넣더라도 `agent-guard.cjs` 가 아니다.** 그건 `.env`·`main` 직접 푸시·`git reset --hard` 만 보는 **안전 훅**이고, 지금 신뢰받는 이유가 위험한 것만 보기 때문이다. 파일 개수가 맞나 같은 위생 검사를 섞으면 성격이 흐려진다. 2.18-draft 에서 `pr-checks.yml` 이 생겼으니 **CI 스텝 3줄**이 더 싼 자리다.
  - **두 번째 사례가 나오면 그때 넣는다.** CHANGELOG 2.17-draft 의 「남은 것」 한 줄을 그렇게 고쳤다 — 다음 사람이 근거 없이 만들지 않도록.
- **진짜 원인은 검사 부재가 아니었다.** 6주간 안 보인 이유는 "세 면에 다 넣는다" 가 **어디에도 안 적혀 있어서**다. 이번에 드리프트를 실제로 잡아낸 경로도 검사가 아니라 [`README.md`](../README.md) 의 "Codex·Copilot 에 같은 워크플로우가 있습니다" 라는 **문장이 거짓이 된 것**이었다. 그 문장이 세 곳(README·AGENTS.md §1·[팀 위키](rules/team-wiki.md))에 있으므로 다음에 어긋나면 또 눈에 띈다.
- 남기지 않은 것: `wiki/log.md` 2026-09-13 항목의 옛 권고는 **고치지 않았다.** 이 기록은 append-only 이고, 그때 그렇게 판단했다는 것 자체가 사실이다. 번복은 이 항목이 한다.

## 2026-09-14 · 고치기 · 본문 여백을 줄였다 (2.20 → 2.21-draft)

- 신고: "각 메뉴에 들어가 보면 오른쪽 본문의 좌우·상하 여백이 너무 크다."
- **증상과 원인이 달랐다.** 여백처럼 보이는 것의 주범은 패딩이 아니라 **`mx-auto max-w-6xl`** 이었다. 1,574px 뷰포트에서 본문 가용 폭 1,334px 을 1,152px 로 깎고 중앙 정렬해, 양쪽에 91px 씩 빈 띠가 생겼다. 패딩 24px 을 더하면 **한쪽 115px**. 패딩만 줄였으면 4px 얻고 끝났을 것이다.
- 정한 것과 이유:
  - **본문 폭 제한을 없앤다.** 표·대시보드는 **폭이 곧 정보량**이다 — [`.agents/ui/DESIGN.md`](../.agents/ui/DESIGN.md) "정보 밀도와 스캔 가능성을 우선한다". 실측 후 본문 폭 1,334px, 우측 빈 띠 0px.
  - **폼은 그 화면에서 감싼다.** 폭 제한을 없애니 설정 화면의 이름·이메일 입력이 **1,260px** 로 늘어났다. 전역으로 되돌리는 대신 `settings.tsx` 만 `max-w-3xl` 로 묶었다 — 같은 DESIGN.md 가 "안정적인 min/max width 를 둔다"고도 말한다. **전역 규칙 하나로 두 요구를 다 만족시키려 하지 않는다.**
  - 나머지는 한 단계씩만 조였다 — 본문 패딩 24→20/16, `Card` 내부 20→16, 페이지 리듬 24→16, 그리드 16→12.
- **절차를 처음으로 실제 실행했다.** 전날 표준으로 넣은 [브라우저 검증 절차](../docs/브라우저-검증-절차.md)를 크롬 headless + CDP 로 돌려 4개 화면의 계산된 여백을 재고 스크린샷을 떴다. 콘솔 오류·4xx/5xx 0건. **문서만 쓰고 안 밟아 봤으면 몰랐을 결함이 절차에 있었다**:
  - **Vite 가 `localhost` 에만 바인딩해 `127.0.0.1` 접속이 거부된다.** 문서는 쿠키 호스트 일치를 이유로 `127.0.0.1` 을 권했는데, **접속 호스트와 서버 바인딩이 먼저 맞아야 한다.** 1단계와 함정 표에 넣었다.
  - 포트도 조용히 바뀐다 — 5173 이 쓰이고 있으면 Vite 가 5174 로 올라간다. 이것도 적었다.
  - 스크립트 쪽 함정: 이미 세션이 있으면 `/login` 이 리다이렉트되어 폼이 없다. 로그인 단계를 조건부로 짜야 한다.
- **이 변경 자체는 절차상 브라우저 검증 대상이 아니다**(문서가 "문구·색·여백만 바꾸는 수정 — 불필요"라고 적었다). 그래도 밟은 이유는 **여백은 눈으로 보는 것 말고 검증 수단이 없어서**다. 테스트·타입 검사가 잡지 못한다. 이 경우를 대상 표에 넣을지는 사례가 더 모이면 정한다.

## 2026-09-14 · 고치기 · 브랜드를 우미 로고 + 표준템플릿 으로 (2.21 → 2.22-draft)

- 계기: "사이드 메뉴 상단 로고가 벌집 모양인데 별로다" → "우미 로고로 바꿔라, 스마트플래너 참고" → "`Woomi Admin` 은 `표준템플릿` 으로".
- 정한 것과 이유:
  - **로고 이미지 위에 색을 덮지 않는다.** 기존 `Boxes` 아이콘은 `bg-primary` 배지 안 흰 아이콘이었는데, 로고 이미지에 그대로 하면 안 된다 — 우미 CI 블루와 UI `--primary` 는 다른 값이라 `filter`·`color` 로 물들이면 브랜드 색이 깨진다. 파생 프로젝트가 `wiki/rules/brand-logo.md` 에 같은 규칙을 두고 있어 그 판단을 그대로 가져왔고, **코드 주석에 박았다**(문서에만 두면 다음 사람이 배지 안에 넣는다).
  - **접힘 상태는 워드마크를 뺀다.** 가로 3:1 로고를 64px 폭에 넣으면 47×16px 이다. 스마트플래너는 정사각 심볼(`sp-icon.svg`)을 따로 두고 좁은 폭에서 교체하는데, **이 저장소엔 그 자산이 없다.** 축소로 두고 "정사각 자산이 생기면 교체" 를 주석에 남겼다 — 없는 자산을 지어내지 않는다.
- **이름을 바꾸니 온보딩이 깨질 뻔했다.** `.claude/commands/onboard.md` 5단계가 "`Woomi Admin` 을 Project name 으로 바꾼다" 고 지시하고 있었다. 문자열 자체가 지시의 일부라 이름을 바꾸면 **찾을 대상이 사라진다.** `grep` 으로 먼저 세고 시작해서 잡았다 — **화면 문구를 바꿀 때는 그 문구를 인용하는 명령·문서를 함께 센다.**
- **로고는 온보딩에서 지우지 말라고 적었다.** 우미 프로젝트면 맞는 자산이고, 아니면 그때 물어본다. 온보딩이 임의로 지우면 브랜드가 빈다.
- 검증: 브라우저에서 **3개 상태**(사이드바 펼침·접힘·로그인 화면)를 봤다. 실측 47×16 / 83×28, 콘솔 오류·4xx/5xx 0건, 이미지 `naturalWidth 166` 정상 로드. **접힘 상태는 이번에 처음 봤다** — 전날 여백 작업에서 "확인하지 못한 것" 으로 남겼던 항목이다.

## 2026-09-14 · 만들기 · 우미 정사각 심볼을 잘라 접힘 사이드바에 (2.22 → 2.23-draft)

- 계기: 전날 "정사각 심볼 자산이 없어 접힘 로고가 47×16 으로 작다" 를 남은 것으로 적었더니, **"우미2 에서 우미 글자를 빼고 동그라미만 크롭해 써라"** 는 지시가 왔다.
- 정한 것과 이유:
  - **눈대중으로 자르지 않았다.** PNG 를 직접 디코드해(zlib + PNG 언필터, 외부 라이브러리 없음) **열별 잉크 픽셀 수**를 셌다. 빈 열 구간이 `(56–65)`·`(115–121)`·`(150–155)` 로 나왔고 첫 구간이 심볼↔워드마크 여백이다. 심볼은 `x 0–55`, 세로도 `y 0–55` — **정확히 56×56 정사각**이라 비율 왜곡 없이 떨어졌다. 배경은 이미 투명이었다(모서리 RGBA `0,0,0,0`).
  - **이미지 라이브러리를 설치하지 않았다.** `Pillow`·`numpy` 가 없는 환경이었는데, 한 번 쓰고 말 작업에 저장소 의존성을 추가하지 않는다. 순수 파이썬 60줄이면 됐다.
  - **줄이는 대신 자산을 바꾼다.** 가로 3:1 워드마크를 48px 폭에 욱여넣는 것이 문제였지 크기 조절이 문제가 아니었다. 스마트플래너가 `sp-icon.svg` 로 같은 해법을 쓴다.
- 실측: 접힘 로고 **47×16 → 28×28**. 펼침은 59×20 그대로.
- **자를 수 있는지 먼저 쟀기 때문에 가능했다.** 만약 심볼이 56×56 이 아니었다면 비율을 맞추느라 여백을 넣거나 잘라내야 했고, 그건 로고 변형이라 함부로 할 일이 아니다. **재 보고 나서 "딱 떨어진다"를 확인한 다음에 잘랐다.**
- 남은 것: PNG 라 고해상도 화면에서 28px 표시에 56px 원본을 쓴다(2배라 충분하다). SVG 가 있으면 더 낫지만 **벡터화는 추정이 들어가므로 하지 않았다** — 원본 벡터 자산이 생기면 그때 교체한다.
